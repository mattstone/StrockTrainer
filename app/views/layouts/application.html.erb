<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Stock Trainer - Learn Trading Through Simulation" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Learn stock trading through gamified simulation. Practice with virtual money, earn XP, unlock achievements, and master the markets risk-free.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app %>

    <%# Include Font Awesome for icons %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  </head>

  <body class="h-full bg-gray-50">
    <% if logged_in? %>
      <!-- Navigation for logged-in users -->
      <nav class="bg-white shadow-lg border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <%= link_to root_path, class: "flex items-center space-x-2" do %>
                <i class="fas fa-chart-line text-2xl text-indigo-600"></i>
                <span class="text-xl font-bold text-gray-900">Stock Trainer</span>
              <% end %>
            </div>

            <!-- Desktop Navigation -->
            <div class="hidden md:flex items-center space-x-8">
              <%= link_to "Dashboard", dashboard_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium #{'bg-indigo-50 text-indigo-600' if current_page?(dashboard_path)}" %>
              <%= link_to "Lessons", lessons_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Trading", trades_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Portfolio", portfolios_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Badges", badges_path, class: "text-gray-700 hover:text-indigo-600 px-3 py-2 rounded-md text-sm font-medium" %>
            </div>

            <!-- User Menu -->
            <div class="flex items-center space-x-4">
              <!-- XP Display -->
              <div class="hidden sm:flex items-center space-x-2 bg-indigo-50 px-3 py-1 rounded-full">
                <i class="fas fa-star text-indigo-600"></i>
                <span class="text-sm font-semibold text-indigo-700"><%= current_user.experience_points %> XP</span>
              </div>

              <!-- Level Badge -->
              <div class="hidden sm:flex items-center space-x-1 bg-green-50 px-3 py-1 rounded-full">
                <i class="fas fa-trophy text-green-600"></i>
                <span class="text-sm font-semibold text-green-700">Level <%= current_user.level %></span>
              </div>

              <!-- Logout -->
              <%= link_to logout_path, method: :delete,
                  class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium",
                  data: { confirm: "Are you sure you want to logout?" } do %>
                <i class="fas fa-sign-out-alt"></i>
                <span class="hidden sm:inline ml-1">Logout</span>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% flash.each do |type, message| %>
      <div class="<%= type == 'notice' ? 'bg-green-50 border-green-200 text-green-800' : 'bg-red-50 border-red-200 text-red-800' %> border-l-4 p-4 mb-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <i class="<%= type == 'notice' ? 'fas fa-check-circle text-green-400' : 'fas fa-exclamation-circle text-red-400' %>"></i>
          </div>
          <div class="ml-3">
            <p class="text-sm font-medium"><%= message %></p>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="<%= logged_in? ? 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8' : '' %>">
      <%= yield %>
    </main>

    <%# Include Chartkick for charts %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chart.js" %>
  </body>
</html>
